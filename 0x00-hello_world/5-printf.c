#include <stdio.h>

/**
 * main - this is the main fumction
 *
 * Return: returns 0 when successful compile
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
